PREFIX : <urn:example:/>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

:SolidApps {
  :app @:SolidApp * ;
}

:SolidApp {
  :website IRI ? ;
  :name xsd:string ? ;
  :description xsd:string ? ;
  :featured xsd:boolean ? ;
  :thumbnail IRI ? ;
}
